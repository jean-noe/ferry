name: Ferry — Audit Daily

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch: {}

concurrency:
  group: ferry-audit-daily
  cancel-in-progress: true

jobs:
  daily-spend-check:
    name: Daily provider spend check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Placeholder — Spend check (Story 8.1)
        run: echo "Daily spend check placeholder"

  prune-processed-events:
    name: Prune ferry-processed-events (>24h)
    needs: [daily-spend-check]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder — Prune dedupe comments (Story 1.3)
        run: echo "Prune placeholder"
